# Apps

Deployable applications are grouped here:

- `api/` - backend API and Socket.io server
- `web/` - main 7MINUTES frontend
- `mockup-sandbox/` - isolated design sandbox

Shared packages stay in `../lib`.
